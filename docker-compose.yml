version: '3.1'

services:
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    ports:
      - "${PORT}:80"

  app:
    build: .
    restart: always
    ports:
      - "8000"
